# Template only. Do not commit populated values and do not add this file to kustomization.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: rxrelay-secrets
  namespace: rxrelay
type: Opaque
stringData:
  database-password: replace-before-apply
  # openfda-api-key: optional-value
